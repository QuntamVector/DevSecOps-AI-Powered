pipeline{
    agent any

    tools {
        maven 'maven'
        jdk 'java-17'
    }

    environment {
        IMAGE_NAME = "manojkrishnappa/devops-pipeline:${GIT_COMMIT}"
    }

    stages{
        stage('Git-Checkout'){
            steps{
                git url: 'https://github.com/QuntamVector/DevSecOps-AI-Powered.git', branch: 'main'
            }
        }

        stage('Compile'){
            steps{
                sh '''
                    echo "Compiling the code..."
                    # Add your compilation commands here
                    mvn compile
                '''
            }
        }

        stage('Build'){
            steps{
                sh '''
                    echo "Building the application..."
                    # Add your build commands here
                    mvn package
                '''
            }
        }

        stage('Docker build'){
            steps{
                sh '''
                    printenv
                    echo "Building the Docker image..."
                    # Add your Docker build commands here
                    docker build -t ${IMAGE_NAME} .
                '''
            }
        }

        stage('Docker Login'){
            steps{
                script {
                    withCredentials([usernamePassword(credentialsId: 'dockerhub-creds', passwordVariable: 'DOCKER_PASSWORD', usernameVariable: 'DOCKER_USERNAME')]) {
                        sh '''
                            echo "Logging into Docker Hub..."
                            echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
                        '''
                    }
                }
            }
        }

        stage('Docker Push'){
            steps{
                sh '''
                    echo "Pushing the Docker image to Docker Hub..."
                    # Add your Docker push commands here
                    docker push ${IMAGE_NAME}
                '''
            }
        }
    }
}